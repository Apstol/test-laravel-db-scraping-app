# БД
test_db

# Таблицы
- incomes
- orders
- sales
- stocks

# Доступ к БД (только операции чтения)

| host         | port | user        | password |
| ------------ | ---- |-------------|----------|
| 88.218.60.111| 3306 | remote_user | n3WQ*eS1 |

# Комментарии
- Данные выгружены по 17-06-2026.
- В некоторых случаях Orders апи в поле total_price возвращает значения вида "900.8499999999999". В базе данных такие значения конвертируются в decimal тип с округлением => 900.85

# Инструкции по запуску контейнера
1. Создать .env: `cd app && cp .env.example .env`
2. `docker-compose up -d`
